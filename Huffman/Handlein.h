#ifndef HANDLEIN_H
#define HANDLEIN_H

#include <Node.h>
#include <fstream>
#include "ListArray.h"

using std::fstream;

class handleIn
{
private:
public:
    handleIn();
    ~handleIn();
     ListArray openFile();




};

#endif // HANDLEIN_H
